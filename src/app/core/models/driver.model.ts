export interface Driver {
    id: number;
    name: string;
    image?: string; // Optional
    jobTitle?: string; // Optional
    yearsOfExperience?: number; // Optional
    avgFuelConsumption?: number; // Optional
    status?: string; // Optional
  }
  